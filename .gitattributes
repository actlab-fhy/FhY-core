# The golden corpora are recorded from the Python oracle by the generators
# beside them, so GitHub collapses them in a diff and leaves them out of the
# repository's language statistics. They stay plain text: a regenerated corpus
# should still be reviewable, just not in the way of the rest of a review.
rust/tests/golden/*.json linguist-generated=true
