[cosmic-ray]
module-path = "src/fhy_core/utils/scope.py"
timeout = 120.0
excluded-modules = []
test-command = "pytest -x --no-header -q -o addopts='' -m 'not slow' tests"

[cosmic-ray.distributor]
name = "local"
